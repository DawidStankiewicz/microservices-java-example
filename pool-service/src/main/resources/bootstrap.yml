spring:
  application:
    name: pool-service
  profiles:
    active: default
  cloud:
    config:
      uri: http://configuration-server:8888
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    org.springframework.web: DEBUG
    com: DEBUG
    dev.stankiewicz: DEBUG
